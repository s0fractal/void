{
  "name": "@void/wasm-builder",
  "version": "0.1.0",
  "description": "WASM builder for Chimera pure function genes",
  "main": "dist/build.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:all": "npm run build && node dist/build.js",
    "test": "npm run build && node dist/build.js --dry-run",
    "clean": "rm -rf dist chimera-output"
  },
  "dependencies": {
    "multiformats": "^13.0.0",
    "@ipld/car": "^5.2.0",
    "commander": "^11.0.0",
    "glob": "^10.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "schemas"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}