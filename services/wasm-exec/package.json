{
  "name": "@void/wasm-exec",
  "version": "0.1.0",
  "description": "Secure WASM executor with policies and metrics",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --watch src --ext ts --exec 'npm run build && npm start'",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@bytecodealliance/wasmtime": "^1.0.0",
    "@void/fnpm-core": "workspace:*",
    "express": "^4.18.0",
    "prom-client": "^15.0.0",
    "winston": "^3.11.0",
    "ndjson": "^2.0.0",
    "rate-limiter-flexible": "^3.0.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "@types/ndjson": "^2.0.0",
    "typescript": "^5.3.0",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}