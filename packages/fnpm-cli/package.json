{
  "name": "@void/fnpm-cli",
  "version": "0.1.0",
  "description": "FNPM CLI with WASM support",
  "type": "module",
  "bin": {
    "fnpm": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/index.js --help"
  },
  "dependencies": {
    "@void/fnpm-core": "workspace:*",
    "commander": "^11.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}