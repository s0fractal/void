# Chimera WASM Integration - Example Configuration
# Copy to .env and adjust as needed

# === Core Feature Flags (ALL DISABLED BY DEFAULT) ===
CHIMERA_ENABLED=0
WASM_EXEC_ENABLED=0
PROTEIN_HASH_ENABLED=0
FNPM_CID_RESOLUTION_ENABLED=0

# === Emergency Controls ===
CHIMERA_FREEZE=0  # Master kill switch - set to 1 to halt all operations

# === Canary Deployment ===
WASM_EXEC_CANARY=0.00  # 0.00-1.00 (0% to 100%)
INTENT_WASM_CANARY=0.00

# === Resource Limits ===
WASM_MAX_MEMORY=1048576    # 1MB default
WASM_MAX_GAS=1000000       # 1M gas units
WASM_MAX_TIME=5000         # 5 seconds
WASM_CAPS=emit             # Allowed syscalls (emit,kv,http,time)

# === IPFS/HTTP Gateways ===
IPFS_GATEWAY_URL=https://ipfs.io
IPFS_FALLBACK_GATEWAYS=https://dweb.link,https://gateway.pinata.cloud

# === Relay Configuration ===
RELAY_PORT=8787
RELAY_RATE_LIMIT=10        # Requests per minute per user
RELAY_IDEMPOTENCY_TTL=300  # 5 minutes

# === Metrics & Monitoring ===
METRICS_ENABLED=1
METRICS_PORT=9090
GRAFANA_URL=http://localhost:3000
PROMETHEUS_URL=http://localhost:9090

# === Security ===
ENABLE_COSIGN_VERIFICATION=0
OPA_POLICY_URL=http://localhost:8181
RESONANCE_HZ=432

# === Development ===
NODE_ENV=development
LOG_LEVEL=info